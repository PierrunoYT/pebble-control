{
  "name": "pebble-control",
  "version": "1.0.0",
  "description": "A focused Windows audio companion for Creative Pebble speakers.",
  "main": "src/main.js",
  "author": "Pebble Control",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check src/lighting.js",
    "icon": "node scripts/make-icons.js",
    "test": "node --test test/*.test.js",
    "dist": "electron-builder --win nsis"
  },
  "dependencies": {
    "loudness": "^0.4.2",
    "node-hid": "^3.4.0"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.pebblecontrol.app",
    "productName": "Pebble Control",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
